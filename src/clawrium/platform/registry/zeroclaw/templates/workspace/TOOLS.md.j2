# TOOLS

Tools available to this agent. ZeroClaw's runtime injects the built-in
toolset; this file documents agent-specific tool preferences and any
custom tools the operator wants surfaced.

## Built-in

- File read/write within the workspace.
- Shell commands (subject to approval policy).
- Provider-side tool calls (per-provider).

## Custom

TBD — add agent-specific tools below.

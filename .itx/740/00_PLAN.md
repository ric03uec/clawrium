## Issue Creation

**Stage**: issue-creation
**Skill**: /itx:issue-new
**Timestamp**: 2026-06-16T00:00:00Z
**Model**: claude-opus-4-7

```prompt
add two issues (feature requests): add support for docker as runtime and add support for smolvm as runtime. for both, the objective is to install the agents inside these runtime containers
```

**Output**: Created #740 — "User can install and run agents inside smolvm microVMs as a runtime target". Companion to #738; both introduce a shared `runtime` concept.
